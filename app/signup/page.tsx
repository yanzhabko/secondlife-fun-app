import Form from "@/components/singup/Form";
import { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = () => {
  return (
    <section>
      <Form />
    </section>
  );
};

export default page;
