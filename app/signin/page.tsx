import Form from "@/components/singin/Form";
import { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = () => {
  return (
    <section>
      <Form />
    </section>
  );
};

export default page;
